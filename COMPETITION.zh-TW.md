# 最短路徑加速競賽 —— 課程公告

*[English version → COMPETITION.md](COMPETITION.md)*

我們給你一支**已經可以正確執行**的最短路徑程式，你的工作是讓它**快很多**，
而且輸出結果一個字都不能變。快多少，就是你的分數：如果你的程式是原本那支的
40 倍快，你的分數就是 40。

## 一次看懂

| | |
|---|---|
| **你要做的事** | 把 `dijkstra_foundation.cpp` 複製成 `solver.cpp`，然後把它加速 |
| **你要繳交的東西** | 兩個檔案：`solver.cpp` 與 `result.json` |
| **繳交網址** | **https://shortestpaths.ccu2026algorithm.workers.dev**（公開排行榜，架在 Cloudflare 上；不必登入、不必 VPN，任何網路都連得到） |
| **截止時間** | **2026 年 9 月 27 日 23:59（台北時間）**，逾時系統直接拒收 |
| **評分測資** | 六筆測資，詳見下方說明 |
| **你的分數** | 六個加速倍率相對於基準程式的**幾何平均** |
| **繳交次數** | 截止前不限次數，取你的**最佳成績**排名 |
| **程式限制** | C++17、只能用標準函式庫、單執行緒、單一檔案 |

需要的東西全都在這個 repository 裡。`README.md` 是正式規格書，本公告則是說明
「為什麼會有這些規則」。

## 1. 題目

`dijkstra_foundation.cpp` 是一支正確但普通的 Dijkstra（二元堆積實作）。它讀入
圖、一次回答一筆查詢、每行輸出一個距離。它很慢——而它就是你的**基準程式
（baseline）**。

你要把它複製成 `solver.cpp` 並加以改良：命令列相同、輸入相同、**輸出必須一模
一樣（byte-identical）**，但時間要更短。這個 repository 裡其他東西都不是你能
改的，尤其**不可以動 `dijkstra_foundation.cpp`**：`grade.py` 會自己重新編譯它
來量測你的基準時間，改了它只會讓你自己想拉高的那個數字變小。

## 2. 四行指令開始

```sh
make foundation                        # 1. 編譯基準程式
scripts/download_large.sh              # 2. 下載評分測資（185 MB，只需一次）
cp dijkstra_foundation.cpp solver.cpp  # 3. 這個檔案就是你的作業
make solver                            #    ...修改 solver.cpp、重新編譯...
python3 grade.py --solver ./solver --instances instances.txt --json result.json   # 4. 評分
```

第 4 步會印出一張表並寫出 `result.json`，其中 `Overall geomean` 那一行就是你的
分數。在你還沒改任何東西之前，這個數字大約是 1.0——因為此時 `solver.cpp` 就是
基準程式本身。

開始之前有兩件事要知道：

- **先確認你的編譯環境**：`make foundation && make check-data`。如果印出來的不是
  `ok`，代表你的工具鏈有問題，先問清楚再往下做。
- **完整評分跑很久。** 未修改的基準程式跑完六筆評分測資需要大約 **1.7 小時的
  CPU 時間**。開發過程請用 repository 內附的小型 dev 測資（約 1.5 分鐘）：
  `python3 grade.py --solver ./solver --instances instances_dev.txt`。
  dev 測資只用來確認正確性與大致速度，**只有 `instances.txt` 會計分**。

## 3. 六筆測資

評分共六筆測資，而且刻意彼此不同：在某張圖上很有效的技巧，換一張圖可能完全沒
用、甚至更慢。其中兩筆屬於 **LOCAL**（以短距離查詢為主），四筆屬於 **GLOBAL**
（以長距離查詢為主）。排行榜會分別顯示兩個分軌平均，但**排名依據是整體
（Overall）平均**。

| # | 測資 | 分軌 | 頂點數 | 邊數 | 查詢數 | 這是什麼圖 | 查詢長什麼樣 |
|---|---|---|---:|---:|---:|---|---|
| 1 | `road2d_large` | GLOBAL | 300,304 | 1,629,732 | 200,000 | 有向道路網，附整數座標；8 % 單行道；分成一般道路／幹道／快速道路三種速度等級 | 目標點分布在 Dijkstra rank 2⁸–2¹⁷（短程到長程都有） |
| 2 | `lattice3d_large` | GLOBAL | 300,763 | 902,289 | 30,000 | 三維環面格子圖，每個頂點分支度固定為 6，**無座標**，邊權重為 log-uniform 1–10⁶ | 均勻隨機的起訖點對 |
| 3 | `local2d_large` | LOCAL | 300,304 | 600,608 | 300,000 | 二維環面格子圖，分支度 4，邊權重均勻分布 1–1000 | 90 % 為短程查詢（目標在 rank 4096 以內），10 % 均勻隨機 |
| 4 | `scalefree_large` | GLOBAL | 300,000 | 873,003 | 50,000 | 有向無尺度圖（冪次律，γ = 2.1），3 % 的頂點是 sink，邊權重 log-uniform 1–10⁴ | 70 % 均勻隨機、30 % 指向前 0.1 % 的樞紐頂點；約 3 % 的目標**無法到達**，必須輸出 `-1` |
| 5 | `hugeq_large` | GLOBAL | 50,176 | 271,492 | 600,000 | 小型道路網，是六筆中最小的一張圖 | 均勻隨機起訖點對，但有 **60 萬筆** |
| 6 | `wide64_large` | LOCAL | 1,999,396 | 3,998,792 | 30,000 | 二維環面格子圖，邊權重高達 10⁸–10⁹；六筆中最大的一張圖 | 目標落在 rank 2¹²–2²⁰；真實距離會**超過 2³²**，用 32 位元存距離會無聲溢位 |

每筆測資中約有 0.1 % 的查詢是 `s == t`，答案為 `0`。

每筆測資在 `instances/` 下有四個檔案：`<name>.graph`、`<name>.queries`、
`<name>.answers`（`grade.py` 用來對答案的正解檔），以及 `<name>.meta.json`，
裡面記錄了 V、E、權重範圍、查詢組成與最大距離。**請務必讀 meta 檔**，那是免費
的情報。

同樣六個家族的 `_dev` 小型版本（約 5 萬個頂點、1 萬筆查詢）已經放在
repository 裡，方便你快速測試。

**最終評分會使用全新產生的測資**：同一份產生器、同一組參數，但**不同的亂數種
子**。凡是依賴釋出檔案「確切內容」的做法都不會有用；依賴「圖的結構」的做法才
會延續。`tools/gen/` 就是真正的產生器，你可以用自己的種子產生練習測資（做法見
README）。

## 4. 分數怎麼算 —— 快幾倍

每一筆測資：

```
加速倍率 speedup = T_base / T_solver
```

`T_base` 是未修改的基準程式、`T_solver` 是你的程式，**兩者都由 `grade.py` 在
你自己的機器上、同一次執行中量測**。分數採用「比值」而不是「時間」正是為了這
一點：機器快慢會被約掉，好機器不會讓你多拿分，舊機器也不會害你扣分。

你的分數是**六個加速倍率的幾何平均**：

```
分數 = (s₁ × s₂ × s₃ × s₄ × s₅ × s₆)^(1/6)
```

所以分數 17.4 的意思就是「平均而言，比一開始拿到的程式快 17.4 倍」。

動手最佳化之前，請先理解三個後果：

- **沒有上限。** 快 400 倍就是 400 分，頂端不會被壓平打結。
- **失敗的測資一律以 0.1 計，而且絕不剔除。** 答案錯誤、逾時、記憶體超標、開了
  多執行緒——六筆裡壞掉一筆，你的乘積就直接乘上 0.1。
- **幾何平均懲罰偏食。** 五筆神速也救不回第六筆。

以下是 README 中的示範結果——四筆很快、一筆普通、一筆答錯：

```
category instance                        T_base   T_solver   peakRSS    speedup  note
GLOBAL   road2d_large.graph             733.1        6.9      210M    106.245  ok
GLOBAL   lattice3d_large.graph         1034.2      412.6      180M      2.506  ok
LOCAL    local2d_large.graph            870.5       14.2      160M     61.303  ok
GLOBAL   scalefree_large.graph          918.0       10.1      190M     90.891  ok
GLOBAL   hugeq_large.graph             1550.3        8.4       40M    184.560  ok
LOCAL    wide64_large.graph                 -          -      900M      0.100  WRONG

Overall                 geomean = 17.361  (n=6)
```

那五筆正確的測資本身值大約 **49 倍**，但一筆答錯就把分數壓到 **17.4 倍**。
**先求正確，再求快。**

### note 欄位的意思

| note | 意思 | 該筆得分 |
|---|---|---|
| `ok` | 正確，完成計時 | `T_base / T_solver` |
| `WRONG` | 輸出與正解不符 | 0.1 |
| `TIMEOUT` | 超過 3600 秒 | 0.1 |
| `MEMORY` | 超過 4 GB | 0.1 |
| `THREADS` | 使用了一個以上的執行緒 | 0.1 |
| `NONDETERMINISTIC` | 三次計時執行的輸出不一致 | 0.1 |
| `CRASH` / `NOOUTPUT` | 非零結束碼，或沒有產生輸出檔 | 0.1 |

### 關於量測的兩個細節

- **`T_base` 是外插估計，不是整份跑完。** 每次評分都讓基準程式跑完 60 萬筆查詢
  等於每筆測資多花半小時，所以 `grade.py` 只用查詢檔的兩段短前綴計時，再擬合
  `T(q) = a + b·q`。誤差只有幾個百分點，而且**對同一筆測資、對每位同學都是同一
  個常數**，在排名中會互相抵消。
- **你的程式會跑三次，取最快的一次。** 機器雜訊本來就有幾個百分點，所以不要追
  求比雜訊還小的改善；評分時請關掉其他程式。

## 5. 競賽規則

你的 `solver.cpp` 必須：

1. 是 **`dijkstra_foundation.cpp` 的修改版**，不能整個重寫。
2. 維持相同命令列：`./solver <graph> <queries> <output>`。
3. 在每一筆測資上產生與基準程式**完全相同**的輸出（不可達輸出 `-1`）。
4. 使用 **C++17，且只能用標準函式庫**。
5. 是**單一檔案**：你寫的所有東西都在 `solver.cpp` 裡，不可以自己另外開標頭檔，
   `#include` 只能引入標準函式庫。
6. 是**單執行緒**。
7. 記憶體低於 **4 GB**，每筆測資時間低於 **3600 秒**。
8. **不得內含預先算好的答案**。

第 3、6、7 條由 `grade.py` 自動強制執行，助教／老師重跑你的程式時也會再檢查一
次。第 1、4、5、8 條則是由人直接閱讀你的 `solver.cpp` 來認定——這正是為什麼上
傳時原始碼要跟成績一起交。

**不可以修改 `dijkstra_foundation.cpp`**，也不可以修改 `grade.py`、正解檔，或
`instances/` 底下的任何東西。

## 6. 在哪裡繳交 —— 排行榜網站

作業一律繳交到課程排行榜，一個架在 Cloudflare 上的小網站：

### **https://shortestpaths.ccu2026algorithm.workers.dev**

只要有連結就能開，不需要帳號、不需要 VPN，在實驗室、家裡或手機上都一樣能用。

#### 排行榜畫面

![排行榜：每位同學一列，含整體、Global、Local 平均與六筆測資的加速倍率](slides/scoreboard.png)

- 標題列顯示目前有多少位同學繳交、總共幾次上傳，以及距離截止的**即時倒數**。
- **Overall / Global / Local** 可切換排名依據。**Overall 才是正式排名**，另外兩
  個是讓你看出自己在哪一個分軌比較弱。
- 灰色那一列固定是 `dijkstra_foundation`，倍率 1.00×，就是你要打敗的基準線。
- 每位同學的列會顯示總分、兩個分軌平均、**六筆測資各自的加速倍率**、嘗試次數
  （*Tries*）與最近一次上傳時間。列是以**學號**標示，**學號會公開顯示**；但你
  的原始碼不會。

#### 上傳表單

![上傳表單：學號、result.json、solver.cpp](slides/submit_form.png)

1. 打開排行榜，點右上角的 **Submit**。
2. 填入**學號**，必須與課程名單上完全一致。
3. 選擇第 4 步產生的 **`result.json`** 與你的 **`solver.cpp`**。
4. 按 **Submit and view ranking**，系統會把你導回排行榜並把你那一列標示出來。

#### 伺服器會檢查什麼

- 伺服器會從 `result.json` 裡的每筆測資時間**重新推導你的分數**再排名。不是
  `grade.py --json` 產生的檔案，或數字兜不起來的檔案，都會被退回並告訴你哪裡有
  問題。
- `result.json` 內記錄了它所量測的那份 **`solver.cpp` 的 SHA-256**，所以兩個檔
  案必須來自**同一次執行**。拿這次的 `result.json` 配上另一次的 `solver.cpp`
  會被拒絕。
- 你的 `solver.cpp` **只有授課教師看得到**，不會出現在排行榜上，也不會給其他同
  學看。
- **想傳幾次就傳幾次。** 每次嘗試都會保留，取最佳成績排名——所以請**早點傳、常
  常傳**。傳壞的一次沒有任何懲罰，而且早點上傳可以先確認整套流程是通的。
- **2026 年 9 月 27 日 23:59（台北時間）** 之後排行榜關閉，一律拒收。不要留到最
  後一小時才動作：光是完整評分本身就可能跑很久。

## 7. 上傳前檢查清單

- [ ] `make check-data` 顯示 `ok`（編譯環境正常）。
- [ ] `solver.cpp` 是基準程式的修改版、單一檔案、C++17、只用標準函式庫、單執行
      緒。
- [ ] `dijkstra_foundation.cpp` 未被修改
      （`git diff dijkstra_foundation.cpp` 沒有任何輸出）。
- [ ] dev 測資全部乾淨：每一列都是 `ok`。
- [ ] 已完整跑完評分測資：`python3 grade.py --solver ./solver --instances
      instances.txt --json result.json`，六列都是 `ok`。
- [ ] 上傳的是**那一次**的 `result.json` 與**那一份** `solver.cpp`。

## 8. 你可能會問

**我的筆電比較舊，會不會吃虧？**
不會。分數是 `T_base / T_solver`，兩個時間都在你自己的機器、同一次執行中量測。
機器慢會讓兩個數字一起變大，比值不變。

**可以用多執行緒、OpenMP 或 GPU 嗎？**
不行。只能單執行緒 C++17、只能用標準函式庫。`grade.py` 會偵測多出來的執行緒，
該筆測資直接以 0.1 計分。

**可以把答案存起來、或預先算好寫進程式裡嗎？**
不行，違反第 8 條。何況最終評分用的是全新產生的測資，這樣做也沒有用。

**可以自己做測資嗎？**
可以，而且建議你做。`tools/gen/` 就是真正的產生器，README 裡有用自己的亂數種子
產生測資並評分的做法。

**每一筆測資都必須正確嗎？**
實務上是的。只要一筆 `WRONG`，大約就會吃掉三分之二的分數（見上面的示範）。

**規則有不清楚的地方怎麼辦？**
請在截止前直接問授課教師，不要自己猜。`README.md` 是正式規格；若本公告與
README 有出入，以 README 為準。

祝順利——並且記得：這份作業真正有趣的地方不在於微調常數，而在於你選擇的**演算
法**。
